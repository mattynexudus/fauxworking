# Runtime output — created by generators at upload time


# Pre-generated test data — run `python3 prebuild.py` to create your own
# rather than sharing one specific run's output. See README.md.


# Credentials — never committed. See nexudus_auth.py.
.env
.env.*

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Local tool settings — machine-specific, not project source
.claude/
